@model Core.Models.CMS.PageSection

<section class="container py-5">
    <div class="row g-5 align-items-center">
        <div class="col-lg-6">
            <img src="@(string.IsNullOrWhiteSpace(Model.ImageUrl) ? "https://picsum.photos/id/30/900/650" : Model.ImageUrl)" class="img-fluid rounded-3 shadow-sm" alt="@Model.Title">
        </div>
        <div class="col-lg-6">
            @if (!string.IsNullOrWhiteSpace(Model.Title))
            {
                <h2 class="fw-bold mb-3">@Model.Title</h2>
            }
            @if (!string.IsNullOrWhiteSpace(Model.Content))
            {
                <div class="text-muted">@Html.Raw(Model.Content)</div>
            }
            @if (!string.IsNullOrWhiteSpace(Model.ButtonText) && !string.IsNullOrWhiteSpace(Model.ButtonUrl))
            {
                <a class="btn btn-dark rounded-pill px-4 mt-3" href="@Model.ButtonUrl">@Model.ButtonText</a>
            }
        </div>
    </div>
</section>
